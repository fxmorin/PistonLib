package ca.fxco.pistonlib.config;

public interface ConfigManagerEntrypoint {

    // proxy-method for entrypoint, maybe should make configManager public instead
    ConfigManager getConfigManager();

}
